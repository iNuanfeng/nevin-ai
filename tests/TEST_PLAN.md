# Nevin AI — 测试计划

> 版本：v0.1
> 更新：2026-06-07
> 覆盖：Phase 0（脚手架）+ Phase 1（后端核心服务）

---

## Phase 0 测试范围（已通过）

参见 tests/TEST_PLAN.md Phase 0 部分。

---

## Phase 1 测试范围

### 1. 项目环境

| 测试项 | 预期结果 |
|--------|----------|
| `npm run dev` 启动 | 0 错误，端口监听正常 |
| DEEPSEEK_API_KEY 配置 | 未设置时 Chat API 返回 500 error 而非崩溃 |

### 2. 数据库 CRUD

| 测试项 | 预期结果 |
|--------|----------|
| profile 自动创建 | 首次读取时自动生成一行空白档案 |
| 外键约束 | 插入无效 mentor_id 时拒绝写入 |
| FTS5 同步 | 写入记忆后，FTS5 能检索到 |
| 数据库索引 | conversations.mentor_id / conversations.deleted / messages.conversation_id 等 |

### 3. Mentor API

| 测试项 | 预期结果 |
|--------|----------|
| GET /api/mentors | 6 位导师，含 system_prompt 和 style_config 字段 |
| PUT /api/mentors/1 | 更新 style_config 后持久化，GET 查得到 |
| 404 处理 | PUT 不存在的 ID 返回 404 |

### 4. Person API

| 测试项 | 预期结果 |
|--------|----------|
| POST /api/persons | 创建联系人，返回 201 + 完整记录 |
| GET /api/persons | 列表返回全部联系人 |
| PUT /api/persons/:id | 部分更新不覆盖未传字段 |
| DELETE /api/persons/:id | 删除后无法再获取 |
| 400 验证 | 无 name 时返回 400 |
| 404 验证 | 操作不存在的 ID 返回 404 |

### 5. Conversation API

| 测试项 | 预期结果 |
|--------|----------|
| POST /api/conversations | 创建对话，返回 201 |
| GET /api/conversations | 列表含最后一条消息预览和导师信息 |
| GET /api/conversations?mentorId=1 | 按导师筛选 |
| GET /api/conversations/:id | 返回对话详情 + 消息列表 |
| DELETE /api/conversations/:id | 软删除（deleted=1），不再出现在列表 |
| 400 验证 | 无 mentorId 时返回 400 |
| 404 验证 | GET/DELETE 不存在的 ID |

### 6. Conversation-Person API

| 测试项 | 预期结果 |
|--------|----------|
| POST /api/conversation-persons | 对话中添加联系人 |
| GET /api/conversation-persons?conversationId=1 | 返回对话关联的联系人 |
| DELETE /api/conversation-persons | 从对话中移除联系人 |
| 400 验证 | 缺少 conversationId 或 personId |
| 幂等性 | 重复添加不报错（INSERT OR IGNORE） |

### 7. Profile API

| 测试项 | 预期结果 |
|--------|----------|
| GET /api/profile | 返回 profile 对象（id=1） |
| PUT /api/profile | 更新字段，不覆盖未传字段 |
| 自动创建 | 空数据库首次 GET 也会自动创建 |

### 8. Memory API

| 测试项 | 预期结果 |
|--------|----------|
| POST /api/memory | 创建记忆，返回 201 |
| GET /api/memory?q=关键词 | FTS5 按内容检索命中 |
| GET /api/memory（空搜索） | 按重要度排列返回 Top-N |
| GET /api/memory?personId= | 按关联人物筛选 |
| GET /api/memory?limit=3 | 限制返回条数 |
| 400 验证 | 无 content 时 POST 返回 400 |

### 9. Chat API — SSE 流式

| 测试项 | 预期结果 |
|--------|----------|
| POST /api/chat 无 API Key | 返回 SSE error 事件，格式正确 |
| SSE 响应头 | Content-Type: text/event-stream |
| SSE 事件格式 | event: xxx 行 + data: JSON 行 |
| 400 验证 | 无 conversationId 或 content 返回 400 |
| 消息持久化 | AI 回复后自动保存到 messages 表 |
| 对话 updated_at | AI 回复后自动更新 |

### 10. Chat Service — System Prompt 组装

| 测试项 | 预期结果 |
|--------|----------|
| System Prompt 模板 | 包含：导师人设 → 风格要求 → 用户档案 → 联系人 → 记忆 → 上下文 |
| 联系人信息注入 | 对话关联的联系人档案完整拼入 prompt |
| 记忆排序 | 按重要度从高到低排列 |
| 空状态 | 无联系人/无记忆时不插入对应章节 |

### 11. 错误处理

| 场景 | 预期 |
|------|------|
| 请求参数缺失 | 400 + { error: "描述" } |
| 资源不存在 | 404 + { error: "not found" } |
| 服务端异常 | 500 + { error: "描述" } |
| SSE 流中错误 | event: error + { error: "描述" } |

---

## 验证方法

- API 自动化验证：通过 curl 脚本逐一测试，记录 pass/fail
- 数据库验证：通过 sqlite3 命令行检查数据状态
- SSE 格式检查：验证 event: / data: 格式 + Content-Type 响应头
- 人工验证：MANUAL_CHECKLIST.md 列出需肉眼确认的项

## 未覆盖范围

- 异步后处理（记忆提炼/联系人丰富）→ 需要 DeepSeek API Key + 实际对话，在集成环境测试
- 图片上传 / 多模态 → Phase 3
- 对话标题自动生成的智能程度 → 功能验证已覆盖，语义评估留待用户验收

---

## Phase 2 测试范围

### 1. 首页（Home Tab）

| 测试项 | 预期结果 |
|--------|----------|
| 顶栏 | 左侧 "Nevin" 渐变标题 + 右侧搜索图标 + 头像入口 |
| 导师过滤器 | 横向滚动 chip，"全部"默认高亮，点击切换筛选 |
| 空状态 | 无对话时显示引导文案：图标 + "还没有对话" + "点击右下角 + 开始吧" |
| FAB 按钮 | 右下角 "+" 按钮，点击弹出导师选择面板 |
| 导师选择面板 | 底部弹出，6 个导师可选，总管家居首带 "默认" 标签 |
| 选择导师后 | 创建对话并跳转到聊天页 |
| 聚焦时的活跃态 | 列表项可点击，点击后跳转聊天页 |
| 删除按钮 | 悬浮显示删除按钮（桌面 hover / 移动端点击） |
| 确认弹窗 | 提示"对话已删除，联系人和记忆不受影响"，确认/取消按钮 |
| Toast 提示 | 底部弹出"对话已删除"提示，3 秒自动消失 |

### 2. 聊天页（Chat Page）

| 测试项 | 预期结果 |
|--------|----------|
| 页面路由 | `/conversations/:id` 加载正确 |
| 聊天头部 | 返回按钮 + 导师图标/名称/title + 添加联系人按钮 |
| 联系人标签栏 | 已关联的联系人以 tag 展示，可移除；带虚线 "+ 添加联系人" 按钮 |
| 消息气泡 | 用户消息蓝色右对齐，AI 回复灰色左对齐，时间戳正确 |
| Markdown 渲染 | 代码块、列表、表格、链接样式正确 |
| 空消息状态 | 新对话显示空的消息区域 |
| 输入框 | 多行 textarea，自动增高（max 100px），Enter 发送，Shift+Enter 换行 |
| 附件按钮 | 输入区左侧图片图标按钮 |
| 发送按钮 | 蓝色圆形，输入为空或流式传输中禁用 |
| SSE 流式 | 逐 token 追加显示，打字指示器动画，完成后刷新消息列表 |
| 错误处理 | 发送失败时显示错误提示消息 |
| 联系人选择器 | 底部弹出 sheet，勾选联系人，确认添加/移除 |
| 加载状态 | 数据加载时显示三点跳动动画 |
| 页面返回 | 返回按钮跳转到首页 |

### 3. 通讯录（Persons Tab）

| 测试项 | 预期结果 |
|--------|----------|
| 顶部标题 | "通讯录" 渐变标题 |
| 搜索框 | 可用，前端实时过滤联系人列表 |
| 联系人列表 | 头像首字渐变背景 + 姓名 + 关系标签 + 背景描述 |
| 新建按钮 | FAB "+" 蓝色按钮，打开底部表单 sheet |
| 新建表单 | 底部 sheet 含姓名/关系选择/背景描述，保存后更新列表 |
| 编辑联系人 | 点击联系人项进入编辑模式，表单回填已有数据 |
| 删除联系人 | 点击 X 按钮直接删除，列表即时刷新 |
| 空列表 | 无联系人时显示空列表 |
| 关系标签颜色 | 同事/恋人/家人/朋友各不同色标 |

### 4. 个人档案（Profile Tab）

| 测试项 | 预期结果 |
|--------|----------|
| 标题 | "个人档案" 渐变标题 + 说明文案 |
| 表单字段 | 6 个字段：名字/背景经历/核心价值观/性格特质/人生目标/生活习惯 |
| 数据回填 | 打开时从 API 加载已有数据回填 |
| 保存按钮 | 调用 PUT /api/profile，显示"保存中…"状态 |
| 保存反馈 | 成功/失败时 toast 提示 |
| 字段类型 | input（名字）/ textarea（其余）区分正确 |

### 5. 导师人设（Mentors Tab）

| 测试项 | 预期结果 |
|--------|----------|
| 标题 | "导师人设" 渐变标题 |
| 导师列表 | 6 位导师，各带对应渐变图标和当前风格描述 |
| 展开编辑 | 点击展开文本编辑器，textarea 带placeholder提示 |
| 保存风格 | 调用 PUT /api/mentors/:id，保存后列表更新 |
| 收起 | 保存后自动收起编辑器，或再次点击收起 |
| 默认风格 | 无定制时显示 "默认风格 — 点击定制" |

### 6. 备份（Backup Tab）

| 测试项 | 预期结果 |
|--------|----------|
| 标题 | "数据备份" 渐变标题 |
| 统计卡片 | 4 格：对话数/联系人数/记忆条数/备份次数 |
| 下载按钮 | 点击触发下载 |
| 状态数据 | 从 API 实时拉取统计数字 |

### 7. 底部导航

| 测试项 | 预期结果 |
|--------|----------|
| 5 个 tab | 首页/通讯录/档案/导师/备份 |
| 图标 | 各 tab 使用对应 lucide 图标 |
| 活跃高亮 | 当前 tab 图标和文字变蓝色 |
| 切换 | 点击后视图切换正确 |

### 8. 响应式

| 测试项 | 预期结果 |
|--------|----------|
| 375px 宽度 | 布局不变形，无水平滚动，字体清晰 |
| 390px 宽度 | iPhone 尺寸下正常显示 |
| 430px 宽度 | 大屏手机尺寸下正常显示 |
| FAB 位置 | 底部导航上方约 32px，不遮挡 |
| 最大宽度 | max-w-[430px] 居中，桌面端有圆角阴影 |

### 9. 设计一致性

| 测试项 | 预期结果 |
|--------|----------|
| 颜色系统 | 背景 #fff，文字 #1d1d1f，强调 #007aff，灰色 #8e8e93/#aeaeb2 |
| 圆角 | 卡片 12-16px，按钮 12px，标签 20px |
| 字体 | -apple-system, SF Pro, PingFang SC |
| 动画 | 底部 sheet slideUp（0.3s），typing 点跳动，toast fadeIn |

### 10. E2E 流程

| 测试项 | 预期结果 |
|--------|----------|
| 创建对话 | 首页 → 点击 + → 选导师 → 跳转聊天页 |
| 发送消息 | 输入文字 → 点击发送 → SSE 流式渲染 |
| 添加联系人 | 聊天页 → 点 + → 选联系人 → 确认 → 标签显示 |
| 通讯录 CRUD | 新建 → 列表显示 → 编辑 → 更新 → 删除 |
| 导师人设定制 | 展开 → 输入风格 → 保存 → 列表更新 |
| 档案编辑 | 填写字段 → 保存 → 重新打开检查持久化 |

---

### 已知非阻塞问题（记录待后续迭代）

**悬空 entities 引用**

- **现象**：删除联系人后，`memories.entities` 中可能残留该联系人的 JSON ID 引用（如 `"[1, 3]"`）
- **原因**：`entities` 是 TEXT/JSON 字段，不是外键约束，不会级联清除
- **影响**：`memory-service.ts` 中 `retrieveRelevant` 的 `personId` 筛选使用 `entities LIKE ?` 匹配，可能返回已删除联系人的记忆
- **建议修复时机**：Phase 3（联系人档案丰富/记忆系统迭代）
- **修复思路**：在 MCP（记忆检索注入）环节加过滤，取到 `entities` 后校验 `person_id` 是否仍存在于 `persons` 表，不存在则跳过该记忆或过滤掉该 ID
- **严重性**：非 blocking，不影响当前功能
